import 'package:flutter/material.dart';

class Tickethp extends StatelessWidget {
  const Tickethp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
            debugShowCheckedModeBanner: false,

     home: AppBar(
       
     )
    );
  }
}
